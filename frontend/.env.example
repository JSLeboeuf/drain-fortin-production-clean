# Frontend Configuration - Copy to .env and fill with your values

# Supabase Configuration
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key_here

# Backend API Configuration
VITE_API_BASE_URL=https://your-api-domain.com
VITE_WS_URL=wss://your-websocket-domain.com

# VAPI Configuration (Public keys only - safe for frontend)
VITE_VAPI_PUBLIC_KEY=vapi_pk_your_public_key_here
VITE_VAPI_WEBHOOK_URL=https://your-project.supabase.co/functions/v1/vapi-webhook

# Environment
VITE_ENVIRONMENT=production
VITE_ENV=production

# Security Configuration
VITE_ALLOWED_ORIGINS=https://your-domain.com,https://www.your-domain.com

# Monitoring (Optional)
VITE_SENTRY_DSN=https://your-sentry-dsn@sentry.io/project-id

# IMPORTANT NOTES:
# - Only VITE_ prefixed variables are exposed to the frontend
# - Never include secret keys (service role, auth tokens) in frontend
# - This file can be committed to git as it contains no secrets
# - Copy to .env.local for local development with real values