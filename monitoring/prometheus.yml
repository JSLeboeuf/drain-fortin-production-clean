# ==========================================
# PROMETHEUS CONFIGURATION
# Drain Fortin Production Monitoring
# ==========================================

global:
  scrape_interval: 15s
  evaluation_interval: 15s

# ==========================================
# RULE FILES
# ==========================================
rule_files: []

# ==========================================
# ALERTING CONFIGURATION
# ==========================================
alerting:
  alertmanagers: []

# ==========================================
# SCRAPE CONFIGURATIONS
# ==========================================
scrape_configs:
  - job_name: 'prometheus'
    static_configs:
      - targets: ['localhost:9090']
    scrape_interval: 5s

# ==========================================
# RECORDING RULES
# ==========================================
recording_rules: []

# ==========================================
# STORAGE RETENTION
# ==========================================
# storage tuned via command args
